# ==============================================
# SUBMAIL CONFIGURATION
# Manage your entire system settings here.
# Copy this file to .env and fill in the values.
# ==============================================

# --- DISCORD AUTHENTICATION ---
# Create an app at https://discord.com/developers/applications
DISCORD_CLIENT_ID=your_client_id_here
DISCORD_CLIENT_SECRET=your_client_secret_here
DISCORD_BOT_TOKEN=your_bot_token_here

# --- SECURITY ---
# Users must be a member of this Discord Server to log in.
# Right-click server icon -> Copy ID (Enable Developer Mode in Discord settings)
DISCORD_GUILD_ID=your_guild_id_here

# --- EMAIL SETTINGS ---
# The domain for your aliases (e.g. example.com)
SMTP_DOMAIN=example.com
SMTP_PORT=25

# --- DKIM SETTINGS (Optional but Recommended) ---
# Generate keys: opendkim-genkey -s default -d yourdomain.com
DKIM_PRIVATE_KEY_PATH="" # Path to private key file, e.g. ./dkim.private.key
DKIM_KEY_SELECTOR="default"


# --- WEB DASHBOARD ---
# URL where your dashboard is hosted
WEB_URL=http://localhost:3000
NEXTAUTH_URL=http://localhost:3000
# Generate a secret: `openssl rand -base64 32` or just smash your keyboard
NEXTAUTH_SECRET=changeme_random_string

# --- DATABASE ---
# Location of your SQLite database
DATABASE_URL="file:../../dev.db" 
# Note: For Monorepo, we use a relative path or absolute path. 
# "file:../../dev.db" ensures both apps find it in the root or packages/db folder.
# Adjust if necessary based on your deployment.
